{
  "name": "ictrade-operation-web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "devbuild": "gulp && mvn clean package -up  -e -U -D maven.test.skip=true -P dev",
    "sitbuild": "gulp && mvn clean package -up  -e -U -D maven.test.skip=true -P sit",
    "uatbuild": "gulp && mvn clean package -up  -e -U -D maven.test.skip=true -P uat",
    "prodbuild": "gulp && mvn clean package -up  -e -U -D maven.test.skip=true -P prod"
  },
  "author": "roy.he@yikuyi.com",
  "license": "ISC",
  "devDependencies": {
    "gulp-cache": "^0.4.6"
  }
}
