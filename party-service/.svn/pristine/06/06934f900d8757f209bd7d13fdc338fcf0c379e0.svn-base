package com.yikuyi.party.partyGroup;


import java.util.List;

import org.junit.Assert;
import org.junit.Test;

import com.yikuyi.party.config.BaseTest;
import com.yikuyi.party.contact.vo.MsgResultVo;
import com.yikuyi.party.group.vo.PartyGroupVo;
import com.yikuyi.party.vo.PartyVo;

public class PartyGroupResourceTest extends BaseTest{
	/**
	 * 根据地址ID查询地址详情
	 * @param ids 需要查询的地址id  eg:122,12512
	 * @param authToken
	 * @return
	 * @since 2017年6月29日
	 * @author tb.yumu@yikuyi.com
	 */
	@Test
	public void orderPermissionsTest() {
		MsgResultVo msg = super.partyClient.partyGroupResource().orderPermissions("9999999901", "YWRtaW46OTk5OTk5OTkwMQ==");
		boolean msgResult= true;
		if(msg!=null){
			msgResult = true;
		}else{
			msgResult=false;
		}
		Assert.assertTrue(msgResult);
	}
	
	/**
	 * 物流公司
	 * @param role
	 * @param partyId
	 * @return
	 * @since 2017年6月30日
	 * @author zr.helinmei@yikuyi.com
	 */
	@Test
	public void getAllPartyGroupListTest() {
		PartyGroupVo partyVo = new PartyGroupVo();
		List<PartyVo> listParty = super.partyClient.partyGroupResource().getAllPartyGroupList(partyVo);
		Assert.assertTrue(listParty.size()>0);
	}
}
